import styles from '../../../styles.json';

export const text = {
	display: {...styles.display},
	typography: {...styles.typography}
}

export const list = {
	display: {...styles.display},
	typography: {...styles.typography}
}

export const image = {
	display: {...styles.display}
}